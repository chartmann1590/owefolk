<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".OwefolkApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_owefolk"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_owefolk"
        android:supportsRtl="true"
        android:theme="@style/Theme.Owefolk">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="owefolk-20260801.web.app" android:pathPrefix="/invite" />
                <data android:scheme="https" android:host="owefolk-20260801.web.app" android:pathPrefix="/__/auth/links" />
            </intent-filter>
        </activity>

        <service
            android:name=".notifications.OwefolkMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
